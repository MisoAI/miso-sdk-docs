<!DOCTYPE html>
<html lang="en">
  <head>
    {% include 'header.njk' %}
  </head>
  <body class="base {{ '__preview__' if data.env === 'preview' }}">
    <header class="navbar navbar-expand-md navbar-dark">
      <nav class="container-xxl flex-wrap flex-md-nowrap" aria-label="Main navigation">
        {% include 'navbar.njk' %}
      </nav>
    </header>
    <div class="container-xxl my-md-4 miso-body-container">
      <aside>
        <nav id="aside-nav" class="collapse" aria-label="Docs navigation">
          {% include 'sidebar.njk' %}
        </nav>
      </aside>
      <main class="order-1">
        <div class="intro ps-lg-4">
          {% if title %}<h1 class="intro-title">{{ title }}</h1>{% endif %}
          {% if description %}<p class="intro-desc">{{ description }}</p>{% endif %}
          {% if title or description %}<hr>{% endif %}
        </div>
        {% include 'section_status.njk' %}
        {#
        <div class="toc mt-4 mb-5 my-md-0 ps-xl-3 mb-lg-5 text-muted">
        </div>
        #}
        <div class="content ps-lg-4">
          {{ content | safe }}
        </div>
      </main>
    </div>
    <footer class="py-5 bg-light">
      {% include 'footer.njk' %}
    </footer>
    {% include 'outro.njk' %}
  </body>
</html>