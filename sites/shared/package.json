{
  "name": "@miso.ai/shared-docs",
  "private": true,
  "scripts": {
    "build:clean": "npx rimraf dist",
    "build": "npm run build:clean && npx sass scss:dist/css",
    "start": "npx sass --watch scss:dist/css"
  }
}
